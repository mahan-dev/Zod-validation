import { CardModule } from "@/module/Card";

const page = () => {
  return (
    <div className="w-full h-[80vh] text-[1.2rem] flex flex-col items-center justify-center">
  
      <CardModule />
    </div>
  );
};

export default page;
